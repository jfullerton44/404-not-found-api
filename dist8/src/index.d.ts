import { SafariApiApplication } from './application';
import { ApplicationConfig } from '@loopback/core';
export { SafariApiApplication };
export declare function main(options?: ApplicationConfig): Promise<SafariApiApplication>;
